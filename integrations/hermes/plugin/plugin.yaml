name: echo-veil-shield
version: "0.7.0"
description: "Required Echo Veil pre-model context and zero-provider-call execution gate for Hermes."
hooks:
  - pre_llm_call
  - on_session_end
  - on_session_finalize
  - on_session_reset
