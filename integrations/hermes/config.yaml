mcp_servers:
  echo-veil:
    command: "echo-veil-agent"
    args: ["--profile", "hermes-qwen3", "--embedder", "ollama", "--embedding-model", "qwen3-embedding:latest", "--embedding-dimension", "1024", "--availability-layer", "mcp"]
    tools:
      include:
        - echo_veil_remember
        - echo_veil_recall
        - echo_veil_forget
        - echo_veil_doctor
